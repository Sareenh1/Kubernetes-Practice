apiVersion: apps/v1
kind: Deployment
metadata:
  name: 
  labels:
    app: 
spec:
  replicas: 2
  selector:
    matchLabels:
      app:
  template:
    metadata:
      labels:
        app: 
    spec:
      containers:
      - name: nginx 1.0
        image:
        ports:
        - containerPort: 80

