apiVersion: apps/v1
kind: Deployment
metadata:
  name: 
  labels:
    app: 
spec:
  replicas: 4
  selector:
    matchLabels:
      app:
  template:
    metadata:
      labels:
        app: 
    spec:
      containers:
      - name: nginx
        image:
        ports:
        - containerPort: 8080
